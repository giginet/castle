---
paths: home/**
---

# homeshick

Files under `home/` are managed by homeshick and symlinked to `~/`.

## Symlinking

Run `homeshick symlink castle` to create symlinks after adding new files. This command requires the files to be committed to git first — uncommitted files will not be symlinked.
